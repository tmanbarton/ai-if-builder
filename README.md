# ai-if-builder
An agent-based interactive fiction builder based on the if-engine Java library I created. Input a file describing the game you want (i.e. map, items, puzzles, etc.) and the ai will produce Java files that use my if-engine for a completed game (aside from the text, which need to be completed separately)
